/* ==========================================================================
 * 内容来源配置
 * --------------------------------------------------------------------------
 * 网站打开时会按顺序尝试下面这些地址，拿到第一个能用的就渲染。
 * 全部失败时，自动使用内置的兜底数据（assets/js/data.js），网站不会白屏。
 *
 * 默认只读同目录的 content.json —— 这个文件跟网站一起部署，最省事最稳。
 *
 * 想做到"改完立刻生效、连部署都不用等"？
 * 把 content.json 额外放一份到 GitHub，然后把它的 raw 地址加进下面的数组，例如：
 *
 *   window.CONTENT_SOURCES = [
 *     'https://raw.githubusercontent.com/你的用户名/仓库名/main/content.json',
 *     'content.json'
 *   ];
 *
 * 注意：GitHub 的 raw 地址国内访问可能不稳定，所以建议两个都留着，
 * 第一个失败会自动用第二个。
 * ========================================================================== */

window.CONTENT_SOURCES = [
  'content.json'
];
